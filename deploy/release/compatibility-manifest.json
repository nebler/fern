{
  "$schema": "./compatibility-manifest.schema.json",
  "schema_version": 1,
  "first_supported_baseline": null,
  "current_release_schemas": {
    "task_store": 1,
    "control_state": 1,
    "github_app_credentials": 1
  },
  "verification": {
    "fresh_schema_test": "internal/compatibility/compatibility_test.go",
    "upgrade_harness": "integration/upgrade/run.sh",
    "rollback_prerequisite": "verified-offline-pre-upgrade-backup"
  },
  "limitations": [
    "No historical taskstore schema is supported before the first release.",
    "Existing development databases from before the schema reset must be deleted and recreated.",
    "The local harness does not claim host, container, or external-service transaction atomicity."
  ]
}
